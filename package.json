{
  "name": "check-hash",
  "version": "0.1.0",
  "description": "test task for skillbox",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "eslint": "./node_modules/.bin/eslint '**/*.{js,ts,tsx}'",
    "eslint:fix": "npm run eslint -- --fix",
    "format": "prettier-eslint '**/*.{js,ts,tsx}' --write"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.(js|jsx|ts|tsx)": [
      "npm run eslint:fix",
      "npm run format",
      "git add"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/serejajar/check-hash.git"
  },
  "keywords": [
    "hash"
  ],
  "author": "Sergey Jar",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/serejajar/check-hash/issues"
  },
  "homepage": "https://github.com/serejajar/check-hash#readme",
  "devDependencies": {
    "eslint": "^7.29.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.23.4",
    "husky": "^6.0.0",
    "lint-staged": "^11.0.0",
    "prettier-eslint": "^12.0.0",
    "prettier-eslint-cli": "^5.0.1"
  }
}
